import { LearnPageLoading } from "@/components/learn/loading-state";

export default function Loading() {
  return (
    <LearnPageLoading
      kicker="Opening HenryCo Learn"
      title="Bringing the academy into view."
      body="Loading live catalog content, learning paths, progress signals, and the next best actions for this session."
    />
  );
}
