import { StaffSurfaceRetired } from "@henryco/ui";

export default function PropertyOwnerRedirectPage() {
  return (
    <StaffSurfaceRetired
      division="HenryCo Property"
      body="Legacy property owner dashboards have been retired while the next premium staff workspace is rebuilt."
      primaryHref="/"
      primaryLabel="Return to Property"
    />
  );
}
