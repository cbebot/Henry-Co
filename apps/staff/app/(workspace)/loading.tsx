/**
 * (workspace) loading boundary — DASH-9 G9 minimal stub.
 */
export default function WorkspaceLoading() {
  return (
    <div
      role="status"
      aria-live="polite"
      style={{ padding: "2rem", textAlign: "center", color: "rgba(10,10,10,0.55)" }}
    >
      Loading…
    </div>
  );
}
