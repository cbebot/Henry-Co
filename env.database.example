# Copy to repo root as `.env.database.local` (gitignored) after filling the real password.
# Supabase → Project Settings → Database → Connection string → URI (postgres user).
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.rzkbgwuznmdxnnhmjazy.supabase.co:5432/postgres
