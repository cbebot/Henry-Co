/**
 * Care (Henry Onyx Fabric Care) widgets barrel.
 *
 * Each widget is a server-rendered presentation component backed by the
 * per-viewer `CareSnapshot`. The module manifest (`../module.tsx`) wires
 * each into a `HomeWidget` entry the shell's home grid renders.
 */

export { BookingsOverviewCard } from "./bookings-overview-card";
export { PaymentsDueCard } from "./payments-due-card";
export { CompletedServicesCard } from "./completed-services-card";
