/**
 * Jobs (Henry Onyx Jobs) widgets barrel.
 *
 * Each widget is a server-rendered presentation component backed by the
 * real `JobsSnapshot`. The module manifest (`../module.tsx`) wires each
 * one into a `HomeWidget` entry the shell's home grid renders.
 */

export { ApplicationsInMotionCard } from "./applications-card";
export { ProfileReadinessCard } from "./readiness-card";
export { SavedRolesCard } from "./saved-roles-card";
export { EmployerOperationsCard } from "./employer-operations-card";
