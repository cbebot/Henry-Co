/**
 * @henryco/gaming-arena/ai — on-device practice opponents + coach math.
 *
 * PURE and client-safe: these run in the browser for solo Practice mode and to
 * power the coach's contextual hints. They consume the same pure rules the
 * server runs, so a practice game is replay-identical to a real one.
 */
export * from "./onyx-lines-ai";
export * from "./onyx-cards-ai";
