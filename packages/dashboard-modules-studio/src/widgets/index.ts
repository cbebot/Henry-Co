/**
 * Studio (Henry Onyx Studio) widgets barrel.
 *
 * Each widget is a server-rendered presentation component. The module
 * manifest (`../module.tsx`) wires each widget into a `HomeWidget` entry
 * the shell's home grid renders, backed by the real per-viewer metrics
 * from `../data`.
 */

export { ProjectsPulseCard } from "./projects-pulse-card";
export { PaymentsDueCard } from "./payments-due-card";
