/**
 * Business (Henry Onyx Business) widgets barrel.
 *
 * Each widget is a server-rendered presentation component. The module
 * manifest (`../module.tsx`) wires each widget into a `HomeWidget` entry
 * the shell's home grid renders.
 */

export { BusinessesCard } from "./businesses-card";
export { ActingContextCard } from "./acting-context-card";
