/**
 * V2-ADDR-01 — Universal entrypoint (types + validation, isomorphic).
 *
 * For server-only helpers (Places API, KYC matcher), import from
 * `@henryco/address-selector/server`.
 *
 * For React components, import from `@henryco/address-selector/client`.
 */

export * from "./types";
export * from "./validate";
